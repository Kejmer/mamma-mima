<%= form_with model: @product do |f| %>
  <div class="row">
    <div class="col-sm-3">
      <%= f.label :name %>
      <%= f.text_area :name, :class => 'form-control' %>
    </div>
  </div>
  <%= f.submit 'Zapisz', :class => "btn-glow btn" %>

<% end %>
